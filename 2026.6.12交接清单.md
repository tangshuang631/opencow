# 2026.6.12 交接清单

## 当前目标

把 OpenCow 推进成可长期个人稳定使用的本地主机智能助手，优先解决本地模型卡死、超时诊断不清、恢复链路弱、可见输出模板化、提权说明不够中文化等问题。

本轮重点：

- 本地模型首 token / 流式超时后，避免用户继续撞同一个失败请求。
- NPC 创建/配置类请求如果被普通本地聊天链路误路由并超时，重试应先转入只读 NPC 草案，不直接写配置、不绕过权限。
- 请求提权、取消提权、重复审批等可见说明尽量中文化。
- 保留权限、确认、审计、回退、无隐藏写入边界。

## 本轮已改

- `apps/desktop/src/app/App.tsx`
  - 新增本地模型超时识别：`streamPhase=waiting-first-chunk`、`streamPhase=streaming`、`maximum execution time`、`timed out`。
  - 新增 NPC 配置意图识别：当失败任务是 `npc-config-write`，或普通 `local-model-chat` 里看起来是 NPC 创建/配置请求时，允许走只读草案恢复。
  - 抽出 `createNpcReadonlyDraftRecoveryState()`，统一 NPC 超时恢复逻辑。
  - `local-model-chat` 失败重试时，如果识别为 NPC 配置类超时，优先切到 `npc-local-collaboration-preview`，不再盲目重新检测 Ollama 后重跑同一聊天请求。

- `apps/desktop/src/features/workbench/workbenchState.permissions.ts`
  - 将高风险确认取消、提权取消、能力变更取消、重复审批跳过的可见恢复说明改为中文。
  - 保留原有排队执行轨迹、回退快照、审计字段，不改变权限决策。

- `apps/desktop/src/features/workbench/components/MainConversation.tsx`
  - 重复审批类型解析兼容 `Approval type:` 和新的 `审批类型:`，避免旧记录或新记录显示断裂。

- `apps/desktop/src/features/workbench/workbenchState.test.ts`
  - 更新提权/取消/重复审批相关断言，保护中文可见说明和恢复可见性。

## 已验证

已通过：

```powershell
npm --workspace apps/desktop exec tsc -b
npm --workspace apps/desktop exec vitest run src/app/app.chat.test.tsx -t "context-length local-model retries" --pool forks --poolOptions.forks.singleFork
npm --workspace apps/desktop exec vitest run src/app/app.task-guard.test.tsx -t "timeout-style failures" --pool forks --poolOptions.forks.singleFork
npm --workspace apps/desktop exec vitest run src/features/workbench/workbenchState.test.ts -t "cancelling a permission upgrade|duplicate pending|cancelled high-risk|capability toggle cancellation|permission cancellation recovery" --pool forks --poolOptions.forks.singleFork
npm --workspace apps/desktop exec vitest run src/features/workbench/components/Inspector.test.tsx -t "cancellation recovery output" --pool forks --poolOptions.forks.singleFork
npm run predesktop:dev
npm run verify:all
```

`npm run verify:all` 结果：609 个 desktop tests、repository build、encoding check、health check、71 个 Tauri tests 全部通过。仅保留已知 Vite chunk size warning。

## 重要风险和未完成

- 本轮对 `local-model-chat` 的 NPC 超时恢复已写入代码，但针对该异常路径的 App UI 回归测试暂未稳定落地；尝试用 fake timers 模拟 480 秒本地模型超时会和 React transition / active task effect 时序纠缠。后续建议抽出更小的纯函数或 hook 层测试，而不是继续在大 App UI 测试里硬推。
- 能力变更取消的 `output.summary` 仍保留一段旧英文主句，审计详情已中文化；如果目标是所有可见层完全中文，需要继续收敛该分支。
- 仍有不少审计 detail 使用英文 trace label，例如 `Queued execution kind`、`Module`、`Source`。它们目前偏调试/审计字段，不是主对话文案；若要彻底中文化，需要统一 trace schema 和兼容旧记录。
- `rag-local-shell-remove-temp-output` 的最终解释链路上一轮未完全打通，后续可以继续调查确认后再加。
- 本地模型输出“尽量都经过大模型处理”的方向要继续保持边界：模型只能解释已验证事实，不能决定权限、不能跳过确认、不能让成功任务因解释失败而失败。

## 下一步建议

1. 先跑完整验证：`npm run predesktop:dev`，再跑 `npm run verify:all`。
2. 给 `App.tsx` 里的 NPC timeout recovery 抽一个可单测的小函数，补上稳定回归，覆盖普通 `local-model-chat` 的 NPC 创建超时转只读草案。
3. 继续中文化提权链路的剩余英文 trace，但不要破坏旧审计记录兼容。
4. 继续把安全成功路径的最终输出接入有界本地模型解释，优先处理项目运行、MCP 插件、NPC 项目截图/展示站点等用户高频路径。
5. 继续压实本地模型超时诊断：区分连接失败、首 token 超时、流式中断、上下文过长、空返回、取消请求。

## 项目文档和规则路径

- 核心规则：`E:\2026\opencow\OPENCOW_CORE_RULES.md`
- 6 月 12 日开发交接：`E:\2026\opencow\2026.6.12开发交接清单.md`
- 本交接清单：`E:\2026\opencow\2026.6.12交接清单.md`
- 总开发文档入口：`E:\2026\opencow\docs\opencow-v1.0-dev-doc.md`
- v1.0 文档目录：`E:\2026\opencow\docs\v1.0`
- 测试验收文档：`E:\2026\opencow\docs\v1.0\08-testing-acceptance.md`
- 本地模型超时/诊断记忆：`E:\2026\opencow\memory\2026-06-12-local-model-timeout-phase-diagnostics-debug-report.md`

## 当前开发规则提醒

- 分支：`dev`
- 不要绕过权限、确认、审计、回退。
- 删除、覆盖、批量写入、启动进程、截图/产物写入等必须保持权限链路。
- 用户可见文案中文优先。
- 本地模型解释必须有超时、取消和 verified facts fallback。
- 启动脚本优先用：`E:\2026\opencow\start-opencow-test.bat`
- 推送若网络失败，才考虑临时使用 Clash Verge 代理 `127.0.0.1:7890`；推送成功后不要留下系统代理。
