# 开发进度清单 2026.6.6-22:48

## 当前结论

- 当前桌面端主线已经从“只会解释”推进到“可规划、可审批、可执行、可验证”的本地助手闭环。
- 最近多轮开发已经实质补齐了三条原先明确缺口：
  - 首页核心对话分流收口
  - 本地项目运行生命周期 `run / status / stop`
  - 对话驱动 Skill 使用闭环与其 app 级显式覆盖
- 这份清单此前已落后于代码与测试状态；本次更新的目标是把后续开发决策基线收回到当前真实状态。
- 当前仍应优先遵循 `E:\2026\opencow\OPENCOW_CORE_RULES.md`。

## 已完成

### P0

- 首页核心对话进一步收口
  - 显式 `workspace` / `project` 总结请求已优先路由到 `workspace-overview`
  - `packages` / `config` 首页请求已直达对应 overview，而不是退回泛化 help
  - `assistant-help-overview` 已收紧为更短的任务导向摘要
  - app 层已显式覆盖普通对话、pending guard、composer 可用性、packages/config/workspace/project routing

- 本地项目运行生命周期补强
  - `workspace-project-run` 已有真实启动链路
  - `workspace-project-stop` 已完成 planner / desktop service / Tauri / app 闭环
  - `workspace-project-status` 已完成 planner / desktop service / Tauri / app 闭环
  - runtime registry 已具备：
    - `version + runs` schema
    - invalid JSON repair
    - legacy bare-array migration
    - `launched_at`
    - `last_status`
    - `last_checked_at`
  - `workspace-project-run` 超时失败已显式进入 task guard UI

- 对话驱动 Skill 使用闭环
  - 已完成 planner / service / app 显式覆盖：
    - `skills-local-install`
    - `skills-local-enable`
    - `skills-local-enabled-list`
    - `skills-local-enabled-match`
    - `skills-local-disable`
    - `skills-local-inspect`
  - 已完成 readonly 本地 RAG 与 skill-assisted readonly RAG app 闭环：
    - `rag-local-doc-search`
    - `skills-local-enabled-rag-doc-search`
  - 已完成 direct skill-assisted shell 主链 app 闭环：
    - `workspace-write` temp-output create
    - `controlled-full + confirmation` destructive remove
  - `app.continuation.test.tsx` 已覆盖 skill-assisted / NPC-assisted preview -> continue 主链

- 自我修复主链第一段真实闭环
  - `opencow-self-repair-preview` 已完成 readonly 预览链路
  - `opencow-self-repair-enabled-skills-registry` 已完成首条 permission-backed mutation repair
  - app 层已覆盖：
    - preview
    - preview -> permission approval -> final repair result
  - 审计与回退可见性已进入结果与测试基线

- 首页任务防卡死与队列收口
  - 本地任务 `attemptCount`
  - 最大尝试次数守卫
  - 最大执行时长守卫
  - 超时失败分流文案
  - 任务队列去重、排队、启动、完成、失败、重试、取消等状态测试

### P1

- `docs/v1.0/08-testing-acceptance.md` 已持续补入最近主链的验收条目
- `docs/v1.0/10-openclaw-adapter.md` 已持续补入最近 adapter 映射与链路说明
- 首页 direct routing、Skill 主链、self-repair mutation、project lifecycle 的测试基线已明显完整于上一阶段

## 未完成

### P0

- 更广的 self-repair mutation 主线仍未展开
  - 当前只完成了一个窄范围的 `enabled-skills.json` 修复动作
  - 更广的 assistant-owned config repair、runtime repair、destructive self-repair 仍未进入真实受控主链

- 当前没有发现第二条同等级、边界同样清晰的新增 P0 代码缺口
  - 首页核心对话、Skill 主链、本地项目运行生命周期都已明显收口
  - 如果下一轮继续开发，应先重新证明存在新的真实行为缺口，再动代码

### P1

- RAG 仍未进入真正“命名知识库 / 导入 / 索引”的产品化阶段
- NPC 仍以 preview、协作说明和 continuation 接入为主，未进入更完整的多步执行主链
- MCP 仍是早期接入，不是完整生态协作
- 首页前端视觉与 Figma 驱动优化仍是后续专项，不是当前主线

### P2

- 历史污染测试文件与清理清单仍需后续专项治理
- 一些 browser preview / compatibility 层仍可后续再做专窗整理和替换

## 当前优先级建议

### 下一窗口优先做什么

1. `P0` 重新评估更广的 self-repair mutation 是否存在一个新的最小真实切片
   - 仍要保持 `preview -> permission / confirmation -> execute -> verify -> audit / rollback` 形状
   - 只接受窄边界、可验证、不会扩成“自动大修”的修复动作

2. 如果没有新的真实 P0 代码缺口，不再继续做低价值 self-repair 文档微调
   - `self-repair` 相关的清单、总览、产品方向、adapter、acceptance 文档现已基本对齐当前实现
   - 下一轮应转入更严格的候选切片审计，或直接转向清单里的下一条真实主线

3. 若必须回到功能开发，优先选择真正未兑现的下一条主线
   - 当前最强候选是 `NPC local project showcase workflow`
   - 现状是：已有 preview、已有验收方向、但仍未进入真实 permission-backed multi-step execution 主链
   - 不回到已收口的 homepage routing
   - 不回到已收口的 `run / status / stop`
   - 不回到已收口的 Skill install / enable / use / readonly RAG 显式覆盖

### 暂时不要扩主线

- 不要把当前窗口重新拉回同类 app 级显式覆盖补点
- 不要把 runtime registry 元数据继续做低价值扩张
- 不要过早切到 NPC showcase 大场景
- 不要先做大范围前端重设计
- 不要在当前主线窗口里顺手清理历史污染文件

## 最近关键检查点

- `777e7f40` `feat: add workspace project stop lifecycle`
- `b05e21d3` `feat: add workspace project status lifecycle`
- `5a72d7bb` `feat: migrate legacy workspace runtime registry`
- `77b89d29` `feat: track workspace runtime launch metadata`
- `6c1c6208` `feat: persist workspace runtime status metadata`
- `a4e1ca40` `feat: persist workspace runtime check metadata`
- `1f591b31` `test: cover project run task guard flow`
- `b86ed276` `feat: tighten homepage assistant routing`
- `6d6ac771` `feat: route generic project summaries to workspace overview`
- `ae085fc2` `test: cover homepage config and packages routing`
- `d2dc176c` `test: cover project status and stop app flows`
- `faf38c81` `test: cover skill-assisted rag app flow`
- `1467a04c` `test: cover readonly rag and skill inspect app flows`
- `6dae9549` `docs: refresh development checklist status`
- `a42fd53b` `docs: align lifecycle verification docs`
- `b52c6a2c` `docs: refine self-repair roadmap and verification`
- `5944613b` `docs: clarify self-repair adapter boundary`
- `31f914c4` `docs: note current self-repair landing state`
- `2c41275a` `docs: align self-repair verification baseline`

## 当前批次验证结果

- 本批是文档收口，不引入新的产品行为
- 需要至少确认：
  - 清单内容与当前 `dev` 分支已验证检查点一致
  - 工作树只包含本次清单更新
  - 不把已完成主线继续错误标记为未完成

## 下个窗口接手提示

- 先读：
  - `E:\2026\opencow\OPENCOW_CORE_RULES.md`
  - `E:\2026\opencow\docs\v1.0\00-overview.md`
  - `E:\2026\opencow\docs\v1.0\08-testing-acceptance.md`
  - `E:\2026\opencow\docs\v1.0\10-openclaw-adapter.md`
  - `E:\2026\opencow\开发进度清单2026.6.6-22-48.md`
- 然后只做两步判断：
  - 当前是否还存在新的真实 P0 行为缺口
  - 如果没有，是否应该继续做文档/验收基线对齐，而不是重复补已存在链路
