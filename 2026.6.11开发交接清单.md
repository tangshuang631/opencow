# 2026.6.11 开发交接清单

## 1. 当前基线

- 仓库路径：`E:\2026\opencow`
- 当前分支：`dev`
- 最新已推送且已验证检查点：`3e92b9f1 fix: harden desktop dev startup freshness`
- 更早的远端参考检查点：`f376627d test: assert readonly npc publish preview stays permission-free`

本清单只把“当前仓内能确认的事实”写清楚，方便其他人先接手。当前工作树里仍有大量未提交草稿，不能默认视为已完成或已验证。

## 2. 本轮已完成并已推送的内容

本轮只完成并推送了一个小而清晰的稳定性批次，目标是减少桌面开发态因为旧前端进程或旧 adapter 产物导致的“看起来没生效 / 明明改了却还是旧行为”的问题。

- 提交：`3e92b9f1 fix: harden desktop dev startup freshness`
- 涉及文件：
  - `package.json`
  - `start-opencow-test.bat`
  - `README.md`
  - `apps/desktop/src/app/app.dev-server-config.test.ts`
- 已落实效果：
  - 桌面启动前强制刷新 `openclaw-adapter` 构建产物
  - `start-opencow-test.bat` 不再默认静默复用一个可能已经陈旧的前端会话
  - 桌面开发启动更偏向“新鲜代码 + 新鲜前端会话”

## 3. 上述已推送批次的验证记录

已跑通：

```powershell
npm --workspace apps/desktop exec vitest run src/app/app.dev-server-config.test.ts src/app/app.chat.test.tsx src/features/ollama/ollamaService.test.ts --runInBand
npm run build
```

说明：

- 这些验证只覆盖上面那个“开发启动新鲜度”小批次。
- 不能据此推断当前整个桌面对话链路已经稳定。

## 4. 当前真正的 P0 主线

当前最高优先级不是 NPC showcase，也不是 git 展示，而是桌面基础链路接近可稳定使用：

- 对话主链路可正常发问、生成、完成、不中途假死
- Ollama / 本地模型链路稳定可用，默认模型为 `gemma4:26b`
- 命令行操作走受控权限申请与执行链路
- 失败可恢复，不要卡死，不要无响应
- 审计与回退保留最小可用骨架
- NPC 配置、Skills、联网搜索、RAG 长文本处理围绕主链路服务，不要喧宾夺主

一句话总结：先把“首页能聊、能跑受控命令、失败可恢复、状态可追踪”做稳，再谈更深的 showcase 自动化。

## 5. 当前已知但尚未完成的重点问题

### 5.1 桌面对话链路仍不稳定

用户最近多次反馈：

- 前端有时“无法对话”
- Ollama 明明在线且 `list` 可见，但应用侧仍超时
- 长问题或长输出时容易中断、截断或看起来卡住
- 曾出现固定话术直接输出、没有真正经过模型的问题，这类行为应全部清理

当前能确认的是：

- 这不是单纯的“检测不到 Ollama”
- 本机某些模型首个有效输出可能非常慢，之前探查中出现过接近 `100s` 才出首块内容的情况
- 已推送的小批次只是降低“启动拿到旧代码/旧前端状态”的干扰，不等于根因已彻底解决

### 5.2 工作树里有大量在途草稿

当前 `git status` 显示大量修改/新增，覆盖范围包括：

- `apps/desktop/src-tauri/*`
- `apps/desktop/src/app/*`
- `apps/desktop/src/features/assistant/*`
- `apps/desktop/src/features/ollama/*`
- `apps/desktop/src/features/workbench/*`
- `packages/openclaw-adapter/src/*`
- `docs/v1.0/*`
- `docs/superpowers/*`

这些内容里混合了：

- P0 对话 / 权限 / 回退 / 审计 / 反卡死相关草稿
- `readonly NPC showcase git confirmation preview` 相关草稿
- 新增测试文件
- 尚未验证的文档补充

结论：

- 这些草稿不能一次性打包提交
- 必须按 P0 主线切小批、先测红、后变绿、再逐步扩大

## 6. 接手时必须先知道的边界

### 6.1 当前不应优先推进的内容

- 不要先深挖 `NPC showcase git confirmation preview`
- 不要直接实现产品内的 `git add / git commit / git push`
- 不要把开发仓库自己的 git 操作和产品内 git 功能混为一谈
- 不要把浏览器预览表现当成桌面成品行为

### 6.2 当前必须坚持的规则

- 规则优先级：用户最新指令 > `OPENCOW_CORE_RULES.md` > `docs/superpowers/openspec-workflow.md` > `docs/v1.0/*`
- 走 `OpenSpec + Superpowers + gstack` 的收敛方式
- 行为改动必须 TDD，至少做到一个最小 red -> green
- 每批只做边界清晰、能单独验证的一小步
- 不要顺手清理大量无关代码
- 不要把全部脏文件一起提交

## 7. 当前仓内值得重点关注的在途文件

以下文件是当前接手人最需要先审的区域：

- `apps/desktop/src/features/ollama/ollamaService.ts`
- `apps/desktop/src/features/ollama/ollamaService.test.ts`
- `apps/desktop/src/features/assistant/localAssistantService.ts`
- `apps/desktop/src/features/assistant/assistantTaskService.ts`
- `apps/desktop/src/features/workbench/workbenchState.ts`
- `apps/desktop/src/features/workbench/workbenchState.ollama.ts`
- `apps/desktop/src/features/workbench/workbenchState.permissions.ts`
- `apps/desktop/src/features/workbench/workbenchState.rollback.ts`
- `apps/desktop/src/features/workbench/components/MainConversation.tsx`
- `apps/desktop/src/features/workbench/components/Inspector.tsx`
- `apps/desktop/src/features/workbench/components/RollbackPanel.tsx`
- `apps/desktop/src/features/workbench/components/Composer.tsx`
- `apps/desktop/src/app/App.tsx`
- `packages/openclaw-adapter/src/localAssistantPlan.ts`
- `packages/openclaw-adapter/src/types.ts`

说明：

- 这些文件不代表都可立即提交
- 只是代表它们已经处于当前主线问题的高相关区域

## 8. 建议接手顺序

### 第一步：重新建立事实基线

先读：

- `OPENCOW_CORE_RULES.md`
- `docs/superpowers/openspec-workflow.md`
- `docs/v1.0/00-overview.md`
- `docs/v1.0/03-model-network.md`
- `docs/v1.0/05-rollback-audit-storage.md`
- `docs/v1.0/07-ui-workbench.md`
- `docs/v1.0/08-testing-acceptance.md`
- `docs/v1.0/10-openclaw-adapter.md`

再执行：

```powershell
git status --short --branch
git log -5 --oneline
```

### 第二步：先收敛“无法对话 / 超时 / 截断”这一条

建议从最小可证实问题开始：

1. 先确认桌面端发送消息后到底有没有真正进入模型调用链
2. 确认超时点在前端等待、适配层、Tauri、还是 Ollama streaming
3. 补一个最小失败测试，只针对当前一个具体症状
4. 用最小修复把这个测试变绿
5. 再跑相邻回归测试

### 第三步：再收敛权限、回退、审计骨架

方向应是：

- 用户发起命令后，是否正确进入受控权限链
- 失败时是否留下可追踪记录
- 回退入口是否收敛到“用户消息下的小图标触发”这一目标形态
- 右侧面板是否只保留必要信息，不重复堆叠整段对话内容

## 9. 当前已知文档状态

- `docs/superpowers/openspec-workflow.md` 已存在，但目前仍是未提交状态
- `docs/superpowers/specs/2026-06-08-npc-showcase-git-confirmation-preview-design.md` 为未提交草稿
- `docs/superpowers/plans/2026-06-08-npc-showcase-git-confirmation-preview.md` 为未提交草稿
- 根目录未发现 `2026.6.8开发清单.md`

因此：

- 后续如果文档中还引用 `2026.6.8开发清单.md`，需要先补真实文件，或把引用改成当前有效清单
- 在补齐前，不要把一个不存在的文件当作正式单一事实源

## 10. 明确禁止事项

- 不要一次性提交当前全部脏工作树
- 不要在未做桌面验证前宣布“对话已修复”
- 不要直接把 git 预览需求扩展成真正提交/推送能力
- 不要为了过测试而删功能
- 不要把 UI 精修放在 P0 对话稳定性之前

## 11. 给接手人的建议结论

当前项目并不是“从零不可用”，而是已经有了不少基础骨架和在途修复，但距离“成熟、稳定、流畅可日常使用”还有明显差距，尤其短板仍集中在：

- 本地模型对话稳定性
- 长输出不中断
- 受控命令执行闭环
- 错误恢复与防卡死
- 右侧信息收纳与回退交互收敛

接手时最重要的不是继续铺新功能，而是把这些 P0 主线按小步 TDD 一条条压实。

## 12. 2026-06-11 当前线程收尾补充

本轮继续按 P0 基础链路推进，没有继续扩大 `NPC showcase git confirmation preview`，也没有实现产品内 `git add / git commit / git push`。

本轮新增的最小可验证改动：

- 在 `apps/desktop/src-tauri/src/ollama.rs` 把 Tauri 侧 Ollama `/api/chat` 请求体构造抽成 `create_ollama_chat_api_request`，让桌面桥接实际发送前端传入的 `numPredict`、`timeoutMs`、`think:false`、`stream:true` 这几个关键字段有独立测试保护。
- 新增 Rust 单测 `chat_api_request_uses_frontend_budget_and_thinking_flags`，覆盖长题请求场景下 `gemma:26b`、`num_predict:4096`、`think:false`、`stream:true` 和原始用户问题会进入 Ollama prompt。
- 这只是桥接边界加固，不等于已经证明真实本机 Ollama 端到端对话完全恢复。

本轮验证记录：

```powershell
cargo test chat_api_request_uses_frontend_budget_and_thinking_flags -- --nocapture
cargo test ollama -- --nocapture
npm --workspace apps/desktop exec vitest run src/features/ollama/ollamaService.test.ts
```

验证结果：

- `chat_api_request_uses_frontend_budget_and_thinking_flags`：通过。
- `cargo test ollama -- --nocapture`：25 个 Ollama 桥接相关测试通过。
- `ollamaService.test.ts`：40 个前端 Ollama service 测试通过。

真实 Ollama 探测状态：

- 本轮尝试读取 `http://127.0.0.1:11434/api/tags`，但第一次被 PowerShell profile/引号污染打断，第二次命令中的 `$_.Exception.Message` 被 shell 展开污染，未得到可靠结果。
- 因此不能在交接时声称“真实本机 Ollama 端到端已验证”。
- 下一位接手如果要继续排查“list 能看到但不能对话”，建议先用无 profile 且正确转义的只读命令重新确认 `/api/tags` 和 `/api/chat`，再启动桌面端复现。

下一步建议优先级：

1. 继续围绕“真实桌面对话能否从输入框进入 Ollama `/api/chat` 并返回最终消息”做最小 TDD，不要先铺 UI 或 NPC 展示。
2. 如果真实 Ollama 可达但桌面仍不能对话，优先核对 Tauri invoke 参数、Rust 请求体、Ollama HTTP 返回体、前端失败归因四个点。
3. 如果长题仍截断，继续围绕 `doneReason: "length"`、自动续写上限、分段补题和用户显式 `继续` 这条链路补测试。
4. 如果要提交，必须先人工 review 当前大量未提交草稿，按 P0 小批次选择性 staging，不要一次性提交整个脏工作树。
