# 2026.6.12 开发交接清单

## 1. 当前基线

- 仓库路径：`E:\2026\opencow`
- 当前分支：`dev`
- 当前工作树仍包含较多未提交草稿，尚未做最终分批整理
- 交接重点：本清单只写“当前能确认的事实”和“后续必须继续推进的 P0 主线”，避免把尚未验证的草稿误写成已完成状态

> 说明  
> 本日进展主要集中在：  
> 1. `Shell / task / self-repair / NPC` 的统一恢复语义收口  
> 2. `start-opencow-test.bat` 的启动稳定性修复  
> 3. `openclaw-adapter` 侧类型/构建错误修复  
> 4. 桌面端长任务、长输入、本地模型中断恢复边界继续收敛  
> 5. 继续推动可稳定使用、可恢复、可审计的 P0 链路

---

## 2. 6.11 之后新增的关键进展

### 2.1 Shell 能力与恢复路径已进入设置页，并开始复用到失败提示

新增的核心变化：

- 新增共享模块：`apps/desktop/src/features/workbench/shellCapability.ts`
- 将 Shell 能力拆成三层：
  - `readonly`
  - `workspace-write`
  - `controlled-full`
- 引入共享说明：
  - `shellCapabilityGroups`
  - `getShellCapabilitySummary()`
  - `getShellRecoveryChecklist()`
  - `getShellDialogRecoveryNarrative()`
- 在 `Workbench.tsx` 的设置页中增加独立区块：
  - `Shell 能力与恢复路径`
- 失败提示中开始统一使用这套恢复叙述，而不是各模块各写一套

影响：

- Shell 能力不再是“藏在代码里的能力”
- 用户能在设置页看到能力边界与恢复路径
- 失败后对话里也会出现统一恢复建议，形成“出错即恢复”的闭环

---

### 2.2 `task / self-repair / shell` 的失败提示开始统一

新增/收口点：

- `apps/desktop/src/app/App.tsx`
  - shell 失败恢复提示开始拼接统一恢复叙述
  - self-repair 失败提示也开始复用统一恢复语义
- `apps/desktop/src/features/assistant/assistantTaskService.ts`
  - shell 执行诊断错误开始附带统一恢复叙述
  - self-repair 诊断错误也开始附带统一恢复叙述
- `apps/desktop/src/features/workbench/workbenchState.tasks.ts`
  - `createAssistantPlanningFailedState(...)`
  - `createDuplicatePlanningFailureSkippedState(...)`
  开始收口统一恢复语义

效果：

- 不同模块不再各自说各自的恢复话术
- 用户看到的恢复逻辑趋于一致：
  - 先判断权限层级
  - 再检查工作区根目录 / 命令白名单 / 快照
  - 再通过对话继续恢复

---

### 2.3 NPC / OpenClaw 适配层开始统一恢复语言

新增/收口点：

- `packages/openclaw-adapter/src/localAssistantPlan.ts`
  - NPC 的多个分支开始统一接入 Shell 恢复叙述
  - 包括：
    - `npc-local-project-showcase-preview`
    - `npc-local-project-run`
    - `npc-local-project-screenshot-capture`
    - `npc-local-project-showcase-site-write`
    - `npc-local-project-showcase-publish-preview`
    - `npc-local-project-showcase-git-confirmation-preview`
- `packages/openclaw-adapter/src/localAssistantPlan.npc-showcase.test.ts`
  - 测试开始验证这些分支的恢复语义
  - 不再只是验证 kind / title，也验证恢复话术是否被统一带入

效果：

- OpenClaw 适配层不只是“产出任务计划”
- 它也开始承担统一恢复语义的输出责任
- 这对后续商用试点很关键，因为它减少了模块间话术分裂

---

### 2.4 启动脚本 `start-opencow-test.bat` 已修复“闪退”问题

问题现象：

- `start-opencow-test.bat` 启动失败后窗口直接闪退
- 看不到失败原因

已做修复：

- 脚本改成出错后不立即退出窗口
- 失败时会保留错误信息，并提示按任意键关闭
- `OpenClaw adapter build` 改走根脚本 `predesktop:dev`
  - 原来直接调用 workspace build
  - 现在统一走根脚本，降低 bat 环境兼容问题

当前状态：

- 失败原因更可见
- 更利于调试桌面启动链路
- 但后续构建错误仍需继续逐层修复，不要只依赖脚本兜底

---

### 2.5 `openclaw-adapter` 的构建类型问题已收敛一轮，但仍需继续验证

在本日中间阶段，`packages/openclaw-adapter` 构建报过以下问题：

1. 跨包引用错误
   - `localAssistantPlan.ts` 里曾直接引用 desktop 包里的恢复叙述函数
   - 在 NodeNext / Node16 模式下，触发了 import 路径问题

2. 联合类型字段访问错误
   - `localAssistantPlan.npc-showcase.test.ts` 中直接访问 `plan.reason / plan.riskSummary / plan.summary`
   - 因为 `LocalAssistantTaskPlan` 是联合类型，必须先对 `plan.kind` 做窄化

已采取的方向：

- 将恢复叙述改成本地常量 `SHELL_DIALOG_RECOVERY_NARRATIVE`
- 在测试中补 `plan.kind` narrowing

交接建议：

- 这部分要继续跑一次完整 build
- 确认当前修复没有引入新的 `tsc` / `vitest` 回归
- 如仍有错误，优先处理 `openclaw-adapter` 的类型严格性，而不是回退统一恢复语义

---

## 3. 当前真正的 P0 主线

当前最高优先级不是继续扩新功能，而是把桌面端最关键的可用性链路稳住：

- 对话主链路可正常发问、生成、完成、不中途假死
- Ollama / 本地模型链路稳定可用
- 长输入 / 长输出不轻易中断
- 命令执行走受控权限申请与执行链路
- 失败可恢复，不要卡死，不要无响应
- 审计与回退保留最小可用骨架
- Shell / task / self-repair / NPC 的失败提示统一语义
- NPC、Skills、联网搜索、RAG 长文本处理围绕主链路服务，不要喧宾夺主

一句话总结：

> 先把“首页能聊、能跑受控命令、失败可恢复、状态可追踪”做稳，再谈更深的 showcase 自动化。

---

## 4. 6.12 新增的 P0 收口方向

### 4.1 长任务 / 长输入

当前判断：

- 长输入已经有分层恢复逻辑
- 本地模型超时、上下文过长、输出过长时，不再只是单点失败
- 目前仍需继续验证：
  - 长输入是否能稳定进入 `Ollama / chat` 链路
  - 超时后是否能正确走自动续写 / 分段 / RAG 辅助
  - 是否会出现“看起来卡住但其实没进入模型”的假恢复

仍需继续关注：

- 长回答保护
- 分段补题
- 用户显式“继续”触发
- context overflow 后的恢复路径

---

### 4.2 本地模型中断

目前已知风险：

- `Ollama` 可见不代表桌面对话一定可用
- 之前发现某些模型首次有效输出可能非常慢
- 可能出现连接正常但首块内容迟到的情况

交接要求：

- 不要只看 `list` 或 `tags`
- 要继续确认：
  - 前端是否真正进入模型调用链
  - Tauri invoke 是否正确
  - Rust 请求体是否正确
  - streaming 是否真的回到前端
  - 超时是否被正确归因

---

### 4.3 日志 / 审计 / 回退继续收口

目前基础设施已经存在：

- `workbenchState.rollback.ts`
  - 回退快照捕获
  - 回退条目记录
  - 快照清理
- `workbenchState.shared.ts`
  - 对话压缩
  - 事件 ID 生成
- `workbenchState.permissions.ts`
  - 审计记录
  - 取消时的恢复可见性
  - 失败时的状态保留

但仍需继续确认：

- 每个失败入口是否都把 audit / rollback 一起挂上了
- 每个取消入口是否保留可恢复信息
- 用户可见输出是否足够简洁，避免诊断信息污染摘要
- 右侧面板是否只保留必要信息，不重复堆整段对话

---

## 5. 当前已知但尚未完全完成的重点问题

### 5.1 `openclaw-adapter` 仍需再跑完整 build / test

本日修过的编译错误包括：

- 跨包 import 问题
- 联合类型属性访问问题

虽然方向已经修正，但交接时仍建议下一位接手立刻跑：

```powershell
npm run predesktop:dev
npm --workspace packages/openclaw-adapter run build
npm --workspace packages/openclaw-adapter exec vitest run
```

并重点确认：

- `localAssistantPlan.ts`
- `localAssistantPlan.npc-showcase.test.ts`
- `types.ts`

是否已完全消除编译错误。

---

### 5.2 `start-opencow-test.bat` 已修，但仍需要实际复跑

当前脚本已避免“失败后闪退”，但仍应确认：

- 根脚本 `predesktop:dev` 是否在当前环境稳定可用
- Ollama 检测是否会误判
- `check` 模式是否按预期停留并输出结果
- `desktop:dev` 是否能稳定拉起桌面端

---

## 6. 当前工作树里仍然需要关注的文件

以下是当前接手人最需要先审的区域：

- `apps/desktop/src-tauri/src/ollama.rs`
- `apps/desktop/src-tauri/src/lib.rs`
- `apps/desktop/src-tauri/src/workspace.rs`
- `apps/desktop/src/app/App.tsx`
- `apps/desktop/src/features/assistant/assistantTaskService.ts`
- `apps/desktop/src/features/assistant/localAssistantService.ts`
- `apps/desktop/src/features/workbench/workbenchState.ts`
- `apps/desktop/src/features/workbench/workbenchState.permissions.ts`
- `apps/desktop/src/features/workbench/workbenchState.rollback.ts`
- `apps/desktop/src/features/workbench/workbenchState.tasks.ts`
- `apps/desktop/src/features/workbench/shellCapability.ts`
- `apps/desktop/src/features/workbench/Workbench.tsx`
- `packages/openclaw-adapter/src/localAssistantPlan.ts`
- `packages/openclaw-adapter/src/localAssistantPlan.npc-showcase.test.ts`
- `packages/openclaw-adapter/src/types.ts`
- `start-opencow-test.bat`

说明：

- 这些文件不代表都能立即提交
- 只是代表它们在当前 P0 主线中非常相关

---

## 7. 建议接手顺序

### 第一步：重新建立事实基线

先读：

- `OPENCOW_CORE_RULES.md`
- `docs/superpowers/openspec-workflow.md`
- `docs/v1.0/00-overview.md`
- `docs/v1.0/03-model-network.md`
- `docs/v1.0/05-rollback-audit-storage.md`
- `docs/v1.0/07-ui-workbench.md`
- `docs/v1.0/08-testing-acceptance.md`
- `docs/v1.0/10-openclaw-adapter.md`

再执行：

```powershell
git status --short --branch
git log -5 --oneline
```

### 第二步：优先确认构建是否恢复

建议立刻跑：

```powershell
npm run predesktop:dev
npm --workspace packages/openclaw-adapter run build
npm --workspace packages/openclaw-adapter exec vitest run
```

重点确认：

- `localAssistantPlan.ts`
- `localAssistantPlan.npc-showcase.test.ts`

是否已完全消除编译错误。

### 第三步：继续收口长任务 / 长输入 / 本地模型中断

建议从最小可证实问题开始：

1. 确认桌面端发送消息后是否真的进入模型调用链
2. 确认超时点在前端等待、适配层、Tauri 还是 Ollama streaming
3. 补一个最小失败测试，只针对当前一个具体症状
4. 用最小修复把这个测试变绿
5. 再跑相邻回归

### 第四步：再收敛权限、回退、审计骨架

方向应是：

- 用户发起命令后，是否正确进入受控权限链
- 失败时是否留下可追踪记录
- 回退入口是否收敛到“用户消息下的小图标触发”这一目标形态
- 右侧面板是否只保留必要信息，不重复堆叠整段对话内容

---

## 8. 当前文档状态与建议

目前已知文档状态：

- `docs/superpowers/openspec-workflow.md` 仍是未提交状态
- `docs/superpowers/specs/2026-06-08-npc-showcase-git-confirmation-preview-design.md` 为未提交草稿
- `docs/superpowers/plans/2026-06-08-npc-showcase-git-confirmation-preview.md` 为未提交草稿

建议：

- 不要把这些文档当作“已经完成的正式交付物”
- 若后续文档继续引用旧的 `2026.6.8开发清单.md`，要先确认真实文件是否存在，或者改写引用
- 在补齐前，不要把不存在的文件当作正式单一事实源

---

## 9. 明确禁止事项

- 不要一次性提交当前全部脏工作树
- 不要在未做桌面验证前宣布“对话已修复”
- 不要直接把 git 预览需求扩展成真正提交/推送能力
- 不要为了过测试而删功能
- 不要把 UI 精修放在 P0 对话稳定性之前

---

## 10. 给接手人的结论

当前项目已经不是“从零不可用”，而是已经有了较多基础骨架与在途修复，但距离“成熟、稳定、流畅可日常使用”仍有明显差距，短板仍集中在：

- 本地模型对话稳定性
- 长输出不中断
- 受控命令执行闭环
- 错误恢复与防卡死
- 日志 / 审计 / 回退一致性
- 桌面启动脚本稳定性
- `openclaw-adapter` 的构建与类型一致性

接手时最重要的不是继续铺新功能，而是把这些 P0 主线按小步 TDD 一条条压实，尤其优先确认：

1. `npm run predesktop:dev` 是否已经恢复
2. `openclaw-adapter` 是否已无 `tsc` 错误
3. `start-opencow-test.bat` 是否不再闪退
4. 本地模型长任务/长输入是否能稳定恢复
5. `Shell / task / self-repair / NPC` 的恢复语义是否仍保持统一

---

## 11. 2026-06-12 交接补充结论

本日实际进展可以总结为：

- `Shell` 能力与恢复路径已经从“文档化”推进到“设置页收纳 + 失败提示复用”
- `task / self-repair / NPC` 的恢复语义已经开始统一，不再各说各话
- `start-opencow-test.bat` 已从“会闪退”改成“会停住输出失败原因”
- `openclaw-adapter` 的编译错误已经定位并修过一轮，但仍建议接手后重新跑完整构建验证
- 长任务 / 长输入 / 本地模型中断仍是 P0 主线，不能因为恢复语义统一就误判为“全部稳定”
