# 后期考虑删除的清单

用途：
- 记录当前阶段确认“对 opencow 真正不必要、且影响运行效率或维护成本”的多余代码、演示逻辑、重复路径或临时兼容层。
- 也记录会反复影响开发效率、开发稳定性、代码整洁度的污染路径，例如终端显示噪音来源、高风险文本块、编码混乱文件、历史遗留兼容层。
- 先登记，不在主线开发中随手大删。
- 后续专门开清理窗口，严格按本清单中的路径和功能说明集中清理。

登记规则：
- 只记录“确实不必要”且“影响效率、稳定性、维护成本或构建体积”的内容。
- 对于污染路径，至少写清“路径 + 当前问题类型 + 是否影响匹配/编码/补丁稳定性 + 后续建议处理方式”。
- 每条都必须写清路径、当前功能、判定原因、清理时机。
- 如果只是“暂时没用上”但后续主线可能接入，不放进这里。
- 如果删除会影响当前主线验证，先不要删，只登记。

建议格式：

```md
## 1. 标题
- 路径：`path/to/file-or-dir`
- 当前功能：……
- 判定原因：……
- 建议清理时机：……
- 备注：……
```

污染路径也可用下面格式：

```md
## X. 污染路径标题
- 路径：`path/to/file-or-dir`
- 当前问题：终端噪音 / 高风险文本块 / 编码混乱 / 匹配不稳 / 历史兼容污染
- 当前功能：……
- 影响：……
- 后续建议：隔离 / 重写 / 删除 / 拆分 / 迁移
- 建议清理时机：……
```

当前记录：
- 暂无

## 1. app.test.tsx line-ending noise
- 路径：`E:\2026\opencow\apps\desktop\src\app\app.test.tsx`
- 当前问题：历史噪音 / 文件尾或换行差异，容易在工作区里持续制造无效 diff
- 当前功能：桌面主链路与权限/确认/任务队列的重要集成测试文件
- 影响：影响 git 观察、文本匹配稳定性与后续清理判断，但当前功能验证仍是主线必需
- 后续建议：在专门清理窗口中只处理换行和文件尾噪音，不与功能改动混做
- 建议清理时机：下一次专门的测试与仓库洁净度清理窗口

## 2. app.test.tsx continuation block encoding pollution
- 路径：`E:\2026\opencow\apps\desktop\src\app\app.test.tsx`
- 当前问题：编码混乱 / 匹配不稳 / 高风险文本块
- 当前功能：桌面端主对话与权限链路的大型集成测试集合
- 影响：局部按钮文本在 continuation 用例里出现乱码，导致精确文本匹配不稳定，增加同文件继续增量编辑的风险
- 后续建议：在专门测试清理窗口里，把 continuation 相关老块迁出或重写到独立干净测试文件，再清掉旧的 skip 块与乱码选择器
- 建议清理时机：下一次专门的测试文件清理与编码稳定性窗口
## 3. assistantTaskService browser preview compatibility layer
- 路径：`E:\2026\opencow\apps\desktop\src\features\assistant\assistantTaskService.ts`
- 当前问题：潜在兼容噪音 / 预览分支膨胀 / 历史过渡层
- 当前功能：为非 Tauri 环境提供 deterministic browser-side preview and fallback results so desktop assistant tests can stay stable
- 影响：当前主线测试依赖它，不能现在删；但随着真实本地执行链继续落地，部分兼容分支可能会逐步变成冗余维护成本
- 后续建议：按 task kind 逐项审查哪些 preview fallback 仍有必要，哪些已经可以收缩、合并或删除
- 建议清理时机：在对应真实桌面执行链与全链路测试已经稳定替代这些兼容分支之后

## 4. app.test.tsx unterminated-string and transform pollution
- 路径：`E:\2026\opencow\apps\desktop\src\app\app.test.tsx`
- 当前问题：编码混乱 / 匹配不稳 / 历史污染 / 当前已出现未闭合字符串导致 transform 失败
- 当前功能：历史大型桌面应用集成测试集合
- 影响：会阻断 `npm --workspace apps/desktop run test:unit -- ...` 这类默认会顺带收集 `src/` 的验证命令，影响新增主线切片的稳定验收节奏
- 后续建议：在专项清理窗口内拆分大文件，把仍有价值的主线用例迁移到多个干净独立测试文件，修复未闭合字符串与污染文本块后，再决定是否继续保留该大文件
- 建议清理时机：下一个专门的测试稳定性与污染治理窗口
